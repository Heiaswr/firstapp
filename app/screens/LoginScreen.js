import React from "react";
import { View, Text } from "react-native";

function LoginScreen(props) {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Login (TODO)</Text>
    </View>
  );
}

export default LoginScreen;
